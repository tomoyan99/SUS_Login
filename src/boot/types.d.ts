export type ViewConfig = {
    defaultWindowSize: {
        width : number,
        height: number
    },
    defaultFontSize: number,
    defaultTerminalSize:{
        cols:number,
        rows:number,
    }
};